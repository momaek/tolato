bin/
.tolato/
.tolato-dev/
*.log
tolato-server
# local config overrides + built binaries (not meant to be committed)
server/config.local.yaml
agent/agent
# docker-compose runtime config (copy of config.example.yaml with real secrets)
/config.yaml
# docker-compose postgres data (bind-mount)
/pgdata/
# server runtime data (geoip mmdb cache, etc.)
/server/data/
/tolato-data/
# Claude Code preview MCP launch config (per-developer)
.claude/launch.json
