services:
  postgres:
    image: postgres:16-alpine
    container_name: tolato-postgres
    environment:
      POSTGRES_USER: tolato
      POSTGRES_PASSWORD: tolato
      POSTGRES_DB: tolato
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
